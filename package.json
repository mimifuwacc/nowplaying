{
  "name": "enhanced-nowplaying",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "format": "pnpm -r run format",
    "lint": "pnpm -r run lint",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish",
    "version:api": "changeset version && pnpm install --lockfile-only",
    "version:extension": "changeset version && pnpm install --lockfile-only"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.11",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "dotenv-cli": "^11.0.0",
    "lefthook": "^1.11.14",
    "prettier": "^3.6.2",
    "typescript": "5.3.3"
  }
}